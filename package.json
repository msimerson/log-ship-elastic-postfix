{
  "name": "log-ship-elastic-postfix",
  "description": "Ship normalized Postfix logs to Elasticsearch",
  "version": "0.9.2",
  "private": false,
  "keywords": [
    "log", "postfix", "elasticsearch", "log ship"
  ],
  "homepage": "https://github.com/DoubleCheck/log-ship-elastic-postfix",
  "bugs": {
    "url": "https://github.com/DoubleCheck/log-ship-elastic-postfix/issues"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/DoubleCheck/log-ship-elastic-postfix.git"
  },
  "dependencies": {
    "elasticsearch": "*",
    "ini": "^1.3.4",
    "moment-timezone": "*",
    "postfix-parser": ">=1.0.2",
    "safe-log-reader": "*"
  },
  "devDependencies": {
    "blanket": "*",
    "coveralls": "*",
    "grunt": "*",
    "grunt-eslint": "*",
    "grunt-mocha-test": "*",
    "grunt-version-check": "*",
    "mocha": "*",
    "mocha-lcov-reporter": "*",
    "travis-cov": "*"
  },
  "config": {
    "blanket": {
      "pattern": [
        "server.js",
        "lib"
      ],
      "data-cover-never": [
        "node_modules",
        "test"
      ]
    },
    "travis-cov": {
      "threshold": 80
    }
  },
  "license" : "BSD-2-Clause",
  "scripts": {
    "coverage": "NODE_ENV=test COVERAGE=1 ./node_modules/.bin/mocha --require blanket --reporter travis-cov",
    "coveralls": "COVERAGE=1 ./node_modules/.bin/mocha --require blanket --reporter mocha-lcov-reporter | ./node_modules/coveralls/bin/coveralls.js",
    "start": "node server.js",
    "test": "NODE_ENV=test mocha"
  }
}
