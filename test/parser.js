'use strict';

var shipper = require('../index');

describe('log-ship-elasticsearch-postfix', function () {
    describe('parser', function () {
        it('configured parser loads', function (done) {

            done();
        });

        it('parses postfix log entries', function (done) {
            done();
        });
    });
});