'use strict';

var shipper = require('../index');

describe('log-ship-elasticsearch-postfix', function () {
    describe('elasticsearch', function () {
        it('elasticsearch module loads', function (done) {

            done();
        });

        it('can connect to elasticsearch', function (done) {
            done();
        });

        it('can store an index map template', function (done) {
        	done();
        });

        it('can save a postfix document', function (done) {
        	done();
        });
    });
});